
[package]
name = "plugin_wire"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", optional = true, features = ["derive"] }
ts-rs = { version = "12.0.1", optional = true }

[features]
default = []
std = ["serde", "ts-rs"]
serde = ["dep:serde"]
